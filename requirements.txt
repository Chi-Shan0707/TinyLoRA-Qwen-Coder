Python 3.10

# 核心训练库 - 必须使用最新版本以确保兼容性
trl >= 0.27.0
transformers >= 4.57.0
accelerate >= 1.12.0
torch >= 2.10.0
peft >= 0.18.0

# 数据处理和模型下载
datasets >= 4.5.0
modelscope >= 1.34.0

# 量化支持
bitsandbytes >= 0.49.0

# 其他依赖
numpy
packaging
pyyaml
regex
requests
tokenizers
safetensors
tqdm
filelock
huggingface-hub