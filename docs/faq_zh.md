# 常见问题（中文）

## 为什么这里 RL 往往比 SFT 更有效？
在极低参数量下，RL 更聚焦“正确性信号”，而 SFT 还要拟合格式与风格噪声。

## 为什么不同次运行结果会波动？
常见原因是：种子设置时机、数据采样顺序、量化模式不一致。

## 为什么出现“无法提取代码”？
优先检查 prompt 一致性，并复用 `utils.py` 中统一的提取逻辑。

## 如何快速调试？
先减小 `MAX_SAMPLES`，减少验证样本，并先跑 baseline 作为参照。
